import ComingSoon from "../components/ComingSoon";

export default function Notifications() {
  return (
    <ComingSoon
      title="Notifications"
      note="No notification backend exists yet — this needs a fresh Notification model/controller/routes. Tell me the trigger events you want (new order, low stock, return request, etc.) and I'll design the schema + endpoints, then wire this page."
    />
  );
}